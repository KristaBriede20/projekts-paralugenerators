[generator]

default_password_length = 8
min_ascii = 65
max_ascii = 126

[database]

create_query = CREATE TABLE IF NOT EXISTS "paroles"("id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT UNIQUE, "garums" INTEGER NOT NULL, "parole" TEXT NOT NULL UNIQUE)
